<project>
  <modelVersion>4.0.0</modelVersion>
  <artifactId>example-webapp</artifactId>
  <groupId>example-webapp</groupId>
  <version>example-webapp</version>
  <name>example-webapp</name>
  <build>
    <plugins>
      <plugin>
        <artifactId>example-webapp</artifactId>
        <version>example-webapp</version>
        <name>example-webapp</name>
        <executions>
          <execution>
            <phase>generate-sources</phase>
            <configuration>
              <tasks>
                <exec
                  dir="${project.basedir}"
                  executable="${project.basedir}/src/main/webapp/index.jsp"
                  failonerror="true">
                </exec>
              </tasks>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
